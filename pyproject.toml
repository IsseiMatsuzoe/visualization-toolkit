[project]
name = "visualization-toolkit"
version = "0.3.0"
description = "Deterministic visual renderers for visual-first teaching"
requires-python = ">=3.10"
dependencies = ["matplotlib>=3.8", "numpy>=1.26"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
